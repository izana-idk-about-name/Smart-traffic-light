from src.interfaces.Isemaforo_controller import Isemaforo_controller

class comunicator(Isemaforo_controller):
    def  semaforo_decision(decision) -> dict:
        decision
        return {"aaaa" : decision}